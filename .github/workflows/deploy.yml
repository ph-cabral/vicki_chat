name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: |
          cd ~/projects/vicki_chat
          git pull
          docker compose up -d --build
